import { css } from '@emotion/core';

export default {
  wrapper: css`
    background-color: #faf7fa;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: calc(100vh - 74px);
    import { css } from '@emotion/core';
  `,
};
